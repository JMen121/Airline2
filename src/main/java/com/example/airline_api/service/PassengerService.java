package com.example.airline_api.service;

import com.example.airline_api.models.Flight;
import com.example.airline_api.models.Passenger;
import com.example.airline_api.repositories.PassengerRepository;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

/*@Service //listen out to service
public class PassengerService {

    @Autowired
    PassengerRepository passengerRepository;
//Autowired is telling the Spring(inversion of control) to inject an instance of the 'ChocolateRepository'
// bean into the 'chocolateRepository' field.
//ChocolateRepository is an interface that extends 'JPARepository'. To be able to interact with the database to do CRUD


public Passenger addNewPassenger(Passenger passenger){
     return passengerRepository.save(passenger);
}




}*/
